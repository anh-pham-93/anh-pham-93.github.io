// @ts-check
import { defineConfig } from 'astro/config';

// site is the production URL. When the custom domain is attached,
// change this one value and add public/CNAME.
export default defineConfig({
  site: 'https://anh-pham-93.github.io',
});
